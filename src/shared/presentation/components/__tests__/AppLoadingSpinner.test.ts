import AppLoadingSpinner from '../AppLoadingSpinner/AppLoadingSpinner';

describe('AppLoadingSpinner', () => {
  it('has displayName set for debugging', () => {
    expect(AppLoadingSpinner.displayName).toBe('AppLoadingSpinner');
  });
});
