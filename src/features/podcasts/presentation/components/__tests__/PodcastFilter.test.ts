import PodcastFilter from '../PodcastFilter/PodcastFilter';

describe('PodcastFilter', () => {
  it('has displayName set for debugging', () => {
    expect(PodcastFilter.displayName).toBe('PodcastFilter');
  });
});
