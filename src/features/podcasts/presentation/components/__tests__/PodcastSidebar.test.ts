import PodcastSidebar from '../PodcastSidebar/PodcastSidebar';

describe('PodcastSidebar', () => {
  it('has displayName set for debugging', () => {
    expect(PodcastSidebar.displayName).toBe('PodcastSidebar');
  });
});
