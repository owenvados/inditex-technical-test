import PodcastList from '../PodcastList/PodcastList';

describe('PodcastList', () => {
  it('has displayName set for debugging', () => {
    expect(PodcastList.displayName).toBe('PodcastList');
  });
});
