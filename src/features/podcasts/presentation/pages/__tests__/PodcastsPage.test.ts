import PodcastsPage from '../PodcastsPage/PodcastsPage';

describe('PodcastsPage', () => {
  it('has displayName set for debugging', () => {
    expect(PodcastsPage.displayName).toBe('PodcastsPage');
  });
});
