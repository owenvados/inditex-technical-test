import EpisodeDetailPage from '../EpisodeDetailPage/EpisodeDetailPage';

describe('EpisodeDetailPage', () => {
  it('has displayName set for debugging', () => {
    expect(EpisodeDetailPage.displayName).toBe('EpisodeDetailPage');
  });
});
