import PodcastDetailPage from '../PodcastDetailPage/PodcastDetailPage';

describe('PodcastDetailPage', () => {
  it('has displayName set for debugging', () => {
    expect(PodcastDetailPage.displayName).toBe('PodcastDetailPage');
  });
});
